{
  "private": true,
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.3",
    "postcss-cli": "^11.0.1",
    "tailwindcss": "^3.4.17"
  },
  "scripts": {
    "build": "NODE_ENV=production && HUGO_ENV=production && hugo --gc --minify -d public",
    "start": "NODE_ENV=development hugo server --disableFastRender"
  }
}
